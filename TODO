- Make an object interface instead of relying on private
  variables for the connection, etc.  This will allow for
  multiple connections to multiple redis instances from a
  single Node.js app.

- Bring up to date with respect to redis HEAD
  - SORT with STORE option (should already work)
  - ZADD
  - ZREM
  - ZRANGE
  - ZREVRANGE
  - ZRANGEBYSCORE
  - ZCARD 
  - ZSCORE
  - SRANDMEMBER
  - MSET
  - MSETNX
